[project]
name = "hail-test-data-generator"
version = "0.1.0"
description = "Generate test Hail tables for decoder development"
requires-python = ">=3.10"
dependencies = [
    "hail",
]

[tool.uv]
dev-dependencies = []
